# The goal of this lecture

- The goal is to learn to train a neural network, with a bit more complex use case
- Also the goal is, the code written in ipynb, can be transferred into a full python file
- regularization can move the point forward, where validation loss starts to increase again
